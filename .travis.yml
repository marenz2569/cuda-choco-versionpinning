language: cpp

os: windows

env:
  jobs:
    - CUDA=6.5
    - CUDA=8.0
    - CUDA=11.0

install:
  - echo $CUDA
  - powershell -noexit "& 'travis-ci\\install-cuda.ps1'"

script:
  - powershell -Command Exit 0
